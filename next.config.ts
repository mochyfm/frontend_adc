import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'picsum.photos'], // añade aquí todos los que quieras usar
  },
};

export default nextConfig;
